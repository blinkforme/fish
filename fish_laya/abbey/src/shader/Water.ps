
varying vec2      v_texCoord;
varying float 	  v_time;
void main(void)
{
  vec2 p =  v_texCoord * 2.0 - 0.5;
  float  len_= length(p)*3.0;
  vec2 uv = v_texCoord + p*0.008*cos((length(p) - v_time)*24.0);
  gl_FragColor = texture2D(CC_Texture0,uv);
 }